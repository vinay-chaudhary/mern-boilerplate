/**
 * Root Reducer
 */
import { combineReducers } from 'redux';

function Reducername(state={}) {
    return state;
}

// Combine all reducers into one root reducer
export default combineReducers({
    Reducername
});
